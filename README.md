# NeoVim Configuration

- [ LSP Config ]( #lsp-config )
    - [Astro](#astro)

## LSP config
```python
import pandas
import numpy
```
### Astro

to make the astro-language-server work you have to run the two following commands inside the astro project directory:

```bash
npm install --save-dev prettier
npm install --save-dev prettier-plugin-astro
```
